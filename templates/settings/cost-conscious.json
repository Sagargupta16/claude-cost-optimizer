{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",

  "_comment": [
    "COST-CONSCIOUS SETTINGS - Aggressive cost savings profile.",
    "Defaults to Haiku for most tasks. Restricts expensive operations.",
    "Best for: solo devs on tight budgets, routine maintenance, simple tasks.",
    "Trade-off: lower quality on complex reasoning, more turns for hard problems.",
    "Copy this file to ~/.claude/settings.json or .claude/settings.json"
  ],

  "model": "haiku",

  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Bash(git status)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(npm test*)",
      "Bash(npm run lint*)",
      "Bash(npm run build*)"
    ],
    "deny": [
      "WebFetch",
      "Bash(npm install*)",
      "Bash(npx*)",
      "Bash(pip install*)",
      "Bash(rm -rf*)",
      "Bash(git push --force*)",
      "Bash(curl*)",
      "Bash(wget*)"
    ]
  },

  "env": {
    "_comment": "Set CLAUDE_CODE_MAX_TURNS to limit runaway sessions",
    "CLAUDE_CODE_MAX_TURNS": "15"
  }
}
