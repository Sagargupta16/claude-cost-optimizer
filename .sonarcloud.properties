# SonarCloud automatic analysis configuration.
#
# The GitHub Action at tools/actions/claude-cost-audit/audit.py is a
# deliberate standalone copy of tools/badge-generator/generate.py (an action
# must be self-contained, no cross-directory imports). Exclude it from
# copy-paste detection only -- every other rule still applies to it.
sonar.cpd.exclusions=tools/actions/claude-cost-audit/audit.py
